{%- comment -%}
  Legacy helper that emitted raw padding custom properties.
  It's now an alias for {% render 'spacing-style' %} so every consumer benefits
  from the base/mobile custom-property pattern.

  Accepts:
    settings: {block.settings || section.settings}
{%- endcomment -%}

{%- render 'spacing-style', settings: settings -%}
